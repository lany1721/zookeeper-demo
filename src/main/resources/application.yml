spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/zookeeper?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 980104
logging:
  level:
    root: info
    cn.zpeace.zookeeper: debug

server:
  port: 8002
